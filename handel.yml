version: 1

name: ben-test-apigateway-app

environments:
  dev:
    app:
      type: apigateway
      proxy:
        path_to_code: .
        runtime: python3.6
        handler: index.handler
        memory: 128
        timeout: 5
        environment_variables:
          MY_FIRST_VAR: pokeID
